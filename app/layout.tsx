import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Flight Search Engine - Find Your Perfect Flight",
  description:
    "Search and compare flights from multiple airlines. Real-time prices, smart filters, and the best deals.",
  keywords: "flights, flight search, cheap flights, airline tickets, travel",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
